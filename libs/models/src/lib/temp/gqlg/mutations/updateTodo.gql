mutation updateTodo($id: ID!, $input: UpdateTodoInput!) {
  updateTodo(id: $id, input: $input) {
    completed
    id
    title
    user {
      email
      id
      name
      phone
      username
      website
    }
  }
}
