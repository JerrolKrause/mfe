query GetUsers($options: PageQueryOptions) {
  users(options: $options) {
    data {
      id
      username
      email
      address {
        geo {
          lat
          lng
        }
      }
    }
    meta {
      totalCount
    }
  }
}
